import { inject } from "@vercel/analytics"
    AOS.init();

    new Typed(".typing", {
      strings: ["Web Developer", "SEO Enthusiast",],
      typeSpeed: 80,
      backSpeed: 40,
      loop: true
    });

    
